import { SeatStatus } from './seat-status';

describe('SeatStatus', () => {
  it('should create an instance', () => {
    const directive = new SeatStatus();
    expect(directive).toBeTruthy();
  });
});
