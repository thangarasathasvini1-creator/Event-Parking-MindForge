import { SeatLabelPipe } from './seat-label-pipe';

describe('SeatLabelPipe', () => {
  it('create an instance', () => {
    const pipe = new SeatLabelPipe();
    expect(pipe).toBeTruthy();
  });
});
